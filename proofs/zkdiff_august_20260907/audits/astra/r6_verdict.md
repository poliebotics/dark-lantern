> Public audit copy, 8 September 2026. Personal identifiers, private instructions and development infrastructure have been
> redacted; scientific findings are preserved. Findings describe the revision reviewed at the time and may be superseded; see
> AUDIT_TRAIL.md.
> Paths are package-relative where the file is in this package and marked (on-box) or (repository root) where it is not; line
> numbers are as at audit time and may have moved.

The proofs pass. The package needs revision before staging.

1. **PASS: batch evidence and numerical traceability.** Fresh verification of the publication files accepted **112/112 proofs**, with every identity check passing. All 112 table rows, 336 framed/raw/public artifact hashes, 16 ladder rows, and the 185-pair and 507-pair agreement summaries reconcile with their underlying records. Counts reproduce: **112 positive, zero clipping, ten mirrors**. The repository ledger verifies all **2,565 entries**, with no missing or additional files. `RESULTS.md` line 311

2. **REVISE: two numerical statements are wrong.** Framed SP1 artifacts vary: **2,444 bytes ×1, 2,445 ×12, 2,446 ×59, 2,447 ×40**. The constant 2,445 in `README.md` line 36 and `PINS.json` line 18 must change. Also, **wrong/correct residual ≈2.3**, whereas **margin/correct residual ≈1.3**; `RESULTS.md` line 118 confuses them. Raw proofs remain 356 bytes and statements 752 bytes. No other numerical mismatch emerged from the checks above.

3. **PASS: the principal claim boundary is honoured.** The README’s opening explanation describes execution binding; its limitations and the shelf disclose calibration use and d2/v10 development validation. The full disclosure names all seven August targets, own/+15 hints, and training-screen consultation. Patent wording uses the filing date; BOSUN receives assistant credit. `README.md` line 14, `CLAIM_BOUNDARY.md` line 23, SHELF_LINE.md:24 (on-box: `SHELF_LINE.md` line 24)

4. **REVISE: material designated on-box is inside both publication payloads.** Actual destructor-panic backtraces appear in repository receipts and R2 node logs. The reviewed payload included infrastructure backtraces requiring sanitisation. Preserve those originals locally and publish an explicit evidence allowlist. Successful rejection-control evidence remains appropriate. The extra synthetic negative outcome expressly described as “published” [private editorial instruction redacted; scientific control findings retained]; retain the mirror-test success and all 112 batch outcomes. Repository incident:35 (on-box: `receipts/gpu0_prove.stderr` line 35), R2 copier:48 (on-box: `build_r2_bundle.py` line 48), `RESULTS.md` line 153

5. **PASS: the principal build pins match.** ELF, vkey, circuit key, constants, expected identities and lockfiles agree with the build evidence and manifests. All first-party Cargo path dependencies exist. Only the two disclosed host-comment substitutions differ from the final source digest list. The full T215855Z record survives locally but is absent from the publication bundles; including a sanitised copy would improve provenance. No fresh build was attempted during this read-only audit. Final node build:52 (on-box: `source/batch/node_records/build_record_20260907T215855Z.txt` line 52)

6. **REVISE: VERIFY’s core instructions are insufficient as printed.** Working-directory changes at lines 64–65 make the subsequent commands at 72 and 82 resolve incorrectly. Anchor every command to a package-root variable or use subshells. Requirements also omit **`protoc` and standard protobuf includes**, required by the SP1 dependency build, plus GNU time/binutils used by the driver. Rehearse the corrected sequence against the exact publication tree with an empty Cargo home. `VERIFY.md` line 57, SP1 dependency build:12 (on-box: `build.rs` line 12)

7. **REVISE: the optional standalone route is external and unpinned.** It clones current ZeeBeam without a commit pin and has not been rehearsed on this package. Vendor the small verifier and lockfile, or pin an external revision and label that dependency explicitly. This does not invalidate the package’s own acceptance verifier, which passed the fresh 112-proof check. `VERIFY.md` line 35

8. **REVISE: oracle regeneration has concrete failures beyond `[confirm]`.** The exporter imports an absent `g1_integer/kernels.py`; `common.py` imports absent `src/train_lean.py`. Their published locations differ. The driver also runs withheld August stages unconditionally. More materially, recalibrating without the withheld August camera rows cannot reproduce the frozen scales underlying the 185-pair integer study. Provide public-subset commands that load frozen calibration/constants, or narrow the regeneration claim. Exporter:26 (`source/tools/export_oracle.py` line 26), `oracle/common.py` line 23, `VERIFY.md` line 189

9. **REVISE: 032 cannot finish preparing its commit.** Its root-ledger generator writes `SHA256SUMS.new` inside the enumerated tree without excluding that file. After renaming it, the ledger references a nonexistent temporary file and verification fails. Generate the ledger outside the tree or exclude the temporary output explicitly. The script itself stops before pushing. 032 script:121 (on-box: `032_zkdiff_darklantern_commit.sh` line 121)

10. **REVISE: the frozen publication text is malformed.** The README patch interpolates the entire 112-row list where a count belongs, producing two approximately 65,000-character prose fields. The commit body still contains **“N proofs, P positive, Z zero, M negative”** because its replacement misses a newline. Both previews retain “DRAFT, unfrozen.” Repair the renderer, inspect its actual output, and refreeze. freeze_transmit.py:42 (on-box: `freeze_transmit.py` line 42), Commit body:7 (on-box: `032_zkdiff_darklantern_commit.body.md` line 7)

11. **REVISE: 033’s download checks and failure semantics do not fulfil its preview.** Downloaded `_control/SHA256SUMS` receives no content comparison, despite the “every object” claim. Upload failure can return exit 1 after partial publication; the following `rclone check` can also terminate after uploading. That contradicts the preview’s “exit 1 means nothing sent” convention. Check every control against embedded pins and classify every post-upload failure as uncertain/partial before issuing a success receipt. 033 script:72 (on-box: `033_zkdiff_r2_bundle.sh` line 72), 033 preview:9 (on-box: `033_zkdiff_r2_bundle.preview.md` line 9)

12. **REVISE: validated bytes can change before consumption.** Ordinary extra files, symlinks and pre-existing hash mismatches are rejected. However, 032 copies after validation and trusts the copied ledger without rechecking its frozen digest; 033 uploads live files after preflight. A concurrent change can therefore enter the prepared commit or reach R2 before detection. Snapshot first, validate that snapshot against embedded pins, then consume it. 032 script:98 (on-box: `032_zkdiff_darklantern_commit.sh` line 98), 033 script:72 (on-box: `033_zkdiff_r2_bundle.sh` line 72)

13. **PASS on R2 consistency; REVISE its ledger description.** All **3,641 payload files / 364,745,911 bytes** match their manifest, including 11 front-matter copies, 2,500 large files and 1,129 node files. Including controls, upload totals are **3,645 files / 367,164,265 bytes**. `draft=false`; the proposed prefix follows the recorded published `results/` convention. However, repository ledgers do not cover every R2-only object as claimed: name the R2 manifest and frozen control digests. R2 README:6 (on-box: `README.md` line 6), Prior publication checks:1 (on-box: `public_verify_021_022_026_20260907T113005Z.txt` line 1)

14. **REVISE operational presentation; no scientific STOP.** Add concise final-run context: eight concurrent processes, **15.30 hours**, median **64.66 minutes per proof**. Attribute contention causally only as an interpretation. Instruction counts were measured for five real rows; shard/chunk counts remain unmeasured and the CUDA cycle limit was requested metadata. Destructor panics followed completed artifacts and do not invalidate independently accepted proofs. Sanitise infrastructure diagnostics and regenerate the publication inventories. `RESULTS.md` line 170, BATCH_REPORT.md:255 (on-box: `source/batch/BATCH_REPORT.md` line 255), BATCH_REPORT.md:330 (on-box: `source/batch/BATCH_REPORT.md` line 330)

15. [Finding on an editorial style rule redacted as private editorial policy; the corrections it asked for were applied to the copied prose.]

16. **TITLE: rank 1, then 2, then 3.** **A Tale of Two Conditionings: PASS**, the strongest combination of description and recognisable allusion. **The Whole Frame and Nothing but the Frame: PASS**, using the secular courtroom formula. **Same Noise, Different Story: REVISE as submitted**: its reference is weaker and “denoises better” should specify the measured noise-prediction residual comparison. Keep candidate 1. TITLE_CANDIDATES.json:7 (on-box: `TITLE_CANDIDATES.json` line 7)

VERDICT: REVISE: exclude on-box incident material from both public payloads and regenerate their frozen inventories before publication.