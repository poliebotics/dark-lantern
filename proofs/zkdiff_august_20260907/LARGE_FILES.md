---
version: 1.4
date: 2026-09-09
status: generated by tools_release/stage_package.py
author: Cathal Ryan Hynes (author of record); drafted with BOSUN, the project's automated research assistant
---

# Large files published on the data layer instead of in this repository

The files below belong to this package by layout but are served from the data layer (bucket `truthbeam`, prefix
`results/zkdiff_august_20260907/v1/`, transmit item 033) because of their size. `LARGE_FILES_SHA256SUMS` beside this
file fixes every one of them by package-relative path and SHA-256 (`sha256sum -c LARGE_FILES_SHA256SUMS` after placing
them at those paths); the data-layer package carries the same bytes under the same relative paths. They are needed for
the oracle parity tests and the oracle regeneration (`VERIFY.md` sections 6 and 7), never for verifying a proof.
`source/oracle_final/` is the export of `oracle/final/vectors/*.npz` by `source/tools/export_oracle.py` and can be
regenerated from the vectors instead of fetched. The 112 raw frames of the proof rows are also on the data layer, under
`frames/`; they are not package files by layout and are listed with their digests in `FRAMES.md`, not here. The offline build
kit under `build_kit/` is described at the end of this file.

Coverage of the ledgers, precisely: this repository's `SHA256SUMS` covers every file in this directory and nothing
outside it. `LARGE_FILES_SHA256SUMS` covers the files listed below, which the data-layer prefix serves at the same
relative paths under `repository_package_large_files/`. The node's proof-run collection under the prefix's
`node_runs/`, the prefix's own `README.md` and its controls are covered by no repository ledger; their authority is the
data layer's `_control/MANIFEST.jsonl` (path, size, SHA-256, object key and content type per object) and its
`sha256sum` form `_control/SHA256SUMS`, whose SHA-256 digests are fixed at publication in the Dark Lantern repository's
root `README.md`, in the provenance paragraph for this package.

## The offline build kit

The same prefix carries `build_kit/`, so the program and its verifiers can be rebuilt with no network (an outside reader's
request, `AUDIT_TRAIL.md` A2 Codex, and Astra round 9): the vendored crates of every lockfile under `source/` and
`tools/standalone_verifier/` (`build_kit/crates/`, with a script that lays them out so the rebuild reproduces the pinned
ELF byte for byte), the pinned toolchain downloads (rustup-init 1.29.1, Rust 1.98.0, cargo-prove and the succinct guest
toolchain of SP1 6.4.0, protoc 21.12, sp1-gpu-server 6.4.0) and the seven Groth16 v6.1.0 circuit files, each with its
SHA-256 in `build_kit/KIT_MANIFEST.json` (26 files, 9,279,782,724 bytes; manifest SHA-256
`6fe432a725e2b81fe8e8815e38200571c9e7393250b17b4bf67182c6b631db46`, also in `PINS.json` `build_kit`). `build_kit/BUILD_KIT.md` gives the offline procedure and the
third-party terms of the kit's contents; `build_kit/offline_build_test/` is the record of the rebuild in a container with
no network (`VERIFY.md` section 2b). The kit is third-party material under its own licences and is not a package file
by layout; it is listed here and in the prefix's `_control/MANIFEST.jsonl`.

| directory | files | bytes |
|---|---:|---:|
| `source/oracle_final/int16_august_650_correct/` | 483 | 32,673,819 |
| `source/oracle_final/int16_august_650_wrong_p2/` | 483 | 32,673,821 |
| `source/oracle_final/int16_d2_1328_correct/` | 483 | 32,673,772 |
| `source/oracle_final/int16_d2_1328_wrong_p2/` | 483 | 32,673,777 |
| `source/oracle_final/int8_august_650_correct/` | 483 | 31,630,786 |
| `source/oracle_final/int8_august_650_wrong_p2/` | 483 | 31,630,789 |
| `source/oracle_final/int8_d2_1328_correct/` | 483 | 31,630,742 |
| `source/oracle_final/int8_d2_1328_wrong_p2/` | 483 | 31,630,745 |
| `oracle/final/august_inputs/` | 112 | 68,125,752 |
| `oracle/final/vectors/` | 8 | 191,548,180 |
| `oracle/rows/` | 2 | 9,060 |
| `oracle/rows/d2/` | 324 | 55,779,840 |
| `oracle/rows/v10/` | 120 | 20,659,200 |
| `oracle/rows_august/` | 2 | 4,640 |
| `oracle/rows_august/august/` | 232 | 39,941,120 |

4664 files, 633,286,043 bytes in all.

## Log

| Version | Date | Author | Change |
|---|---|---|---|
| 1.0 | 2026-09-07 | BOSUN | Generated with the first staging run. |
| 1.1 | 2026-09-08 | BOSUN | Coverage of the repository and data-layer ledgers stated (Astra round 6, finding 13). |
| 1.2 | 2026-09-08 | BOSUN | The August camera-derived tensors added to the data-layer set (the principal's decision of 8 September 2026); the frames directory pointed to FRAMES.md. |
| 1.3 | 2026-09-08 | BOSUN | The cached-row directories renamed without the private cache prefix; the offline build kit described (Codex round 2). |
| 1.4 | 2026-09-09 | BOSUN | authorship line, 9 September 2026. |
